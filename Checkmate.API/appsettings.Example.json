{
  "ConnectionStrings": {
    "Docker": "Connection string to the docker SQL Server"
  },
  "AllowedHosts": "*",
  "Jwt": {
    "Key": "The super long JWT secret Key because it must be hard to find the super long JWT secret key",
    "Issuer": "Issuer name",
    "Audience": "Audiance name"
  },
  "GoogleCaptcha": {
    "SecretKey": "Google Captcha secret key"
  }
}
